{
  "name": "common-colors",
  "version": "1.0.0",
  "description": "A package that analyses your color usage in styling files",
  "main": "index.js",
  "scripts": {
    "test": "jest --config ./jest.config.json --coverage",
    "test-watch": "jest --config ./jest.config.json --watchAll --coverage"
  },
  "bin": {
    "most-common-color": "src/mostCommonColor.js"
  },
  "keywords": [
    "color",
    "common",
    "styling"
  ],
  "author": "Rosie Watson",
  "license": "ISC",
  "dependencies": {
    "chalk": "^4.1.0"
  },
  "devDependencies": {
    "jest": "^26.1.0"
  }
}
